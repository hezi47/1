#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
{
	const int amount = 100;
	int price = 0;
	printf("请输入金额（元）");
  scanf_s("%d", &price);
	int change = amount - price;
	printf("找您%d元。\n",change);

	return 0;
这里的("找您%d元。\n",change)     、
}
